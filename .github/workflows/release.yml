name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, 'chore(release)')"
    permissions:
      contents: write
      id-token: write
      issues: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - run: npm install -g npm@latest
      - run: npm --version

      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm run lint
      - run: npm test
      - run: npm run build

      - name: Get pre-release version
        id: before
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Release
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to npm
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if [ "$VERSION" = "${{ steps.before.outputs.version }}" ]; then
            echo "No new version released, skipping npm publish"
            exit 0
          fi
          if npm view "pinchtab-mcp@$VERSION" version 2>/dev/null; then
            echo "Version $VERSION already published on npm, skipping"
          else
            echo "Publishing $VERSION to npm"
            npm publish --provenance --access public
          fi
